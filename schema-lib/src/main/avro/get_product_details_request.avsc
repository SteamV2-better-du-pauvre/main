{
  "type": "record",
  "name": "GetProductDetailsRequest",
  "namespace": "com.steam_v2_better_du_pauvre.catalog",
  "fields": [
    { "name": "requestId", "type": {"type": "string", "logicalType": "uuid"} },
    { "name": "playerId", "type": {"type": "string", "logicalType": "uuid"} }, // Pour savoir si on possède déjà les DLCs
    { "name": "productId", "type": {"type": "string", "logicalType": "uuid"} } // ID du jeu cliqué
  ]
}